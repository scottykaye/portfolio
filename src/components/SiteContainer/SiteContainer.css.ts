import {style} from '@vanilla-extract/css';
import {theme} from '@/hooks/theme/theme.css';

export const SiteContainerStyles = style({
  display: 'flex',
  flexDirection: 'column',
  minHeight: '100%',
});
